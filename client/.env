# Build-time config for the static marketing site (Vite %VITE_*% HTML replacement).
# Default = production. Deploys (bootstrap.sh / CI) override per environment.
VITE_LEARN_URL=https://learn.musilinda.com
